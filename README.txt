Create boxes using your mouse with javaFX.
To run using eclipse, just right click main.java and run as java app
To export a executable jar, run a maven build with goals set to package ( run twice if failed on first attempt).
